<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="LOG_FORMAT" source="logging.format" defaultValue="json"/>
    <springProperty scope="context" name="LOG_LEVEL" source="logging.level.root" defaultValue="INFO"/>

    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>request_id</includeMdcKeyName>
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <customFields>{"service":"backend"}</customFields>
        </encoder>
    </appender>

    <appender name="CONSOLE_TEXT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [request_id=%X{request_id:-none}] - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="debug | dev">
        <root level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE_TEXT" />
        </root>
    </springProfile>

    <springProfile name="!(debug | dev)">
        <root level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE_JSON" />
        </root>
    </springProfile>
</configuration>
